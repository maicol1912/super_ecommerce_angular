import { environment } from 'src/environments/environment';

/**
 * Used to redirect between own front
 * Object
 */
export const PageResources = {
  listProducts: '/dash/products',
  login: '/auth/login',
  completeData: 'auth/signin',
  resetPassword: 'auth/forgot',
  dashboardStatus: 'app',
};
